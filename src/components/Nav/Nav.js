import React, { Component } from 'react';

import { StyledNav } from './StyledNav';

const Nav = () => (
  <StyledNav className="bg-light justify-content-between">
    <div />
    <div />
  </StyledNav>
);

export default Nav;
